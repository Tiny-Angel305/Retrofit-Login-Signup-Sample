<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jlynn.psychic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PsychicContact">
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/Theme.PsychicContact.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:theme="@style/Theme.PsychicContact.Splash" />
        <activity
            android:name=".ui.SignupActivity"
            android:label="@string/lb_registration"
            android:theme="@style/Theme.PsychicContact.Main" />
        <activity
            android:name=".ui.ForgotPasswordActivity"
            android:label="@string/lb_forgot_pass"
            android:theme="@style/Theme.PsychicContact.Main" />
        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/lb_home"
            android:theme="@style/Theme.PsychicContact.Main" />
    </application>

</manifest>